name: Secret Scanning

on:
  push:
    branches: [main]
  pull_request:
  # Required checks must name the merge queue's event too, or the queue waits
  # forever for a check that never starts.
  merge_group:

permissions:
  contents: read

env:
  # The gitleaks CLI, pinned and checksum-verified, the same way the docs job
  # pins other tools. NOT gitleaks/gitleaks-action: the action refuses to run for an
  # organization-owned repository without a paid licence key
  # ("[managoat] is an organization. License key is required."), which is how
  # this job failed the moment the repo moved out of a user account (0048).
  # The scanner itself is MIT and unmetered; only the action wrapper is
  # licensed, so the gate survives by dropping the wrapper.
  GITLEAKS_VERSION: 8.30.1
  GITLEAKS_SHA256: 551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb

jobs:
  gitleaks:
    name: Detect secrets
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4
        with:
          # The scan is a commit range, so both ends have to be reachable.
          fetch-depth: 0

      - name: Install gitleaks
        run: |
          set -euo pipefail
          curl -fsSL -o gitleaks.tar.gz \
            "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz"
          echo "${GITLEAKS_SHA256}  gitleaks.tar.gz" | sha256sum -c -
          tar -xzf gitleaks.tar.gz gitleaks
          ./gitleaks version

      - name: Resolve the range to scan
        id: range
        env:
          # A merge group has a base commit the same way a PR does, so both
          # take the base..HEAD branch below.
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha }}
          BEFORE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          # A pull request or a merge group scans base..HEAD, which is what the
          # action did. A push to main scans what it pushed — unless `before`
          # is unreachable (a force push, or the zero sha on a new branch),
          # where the only honest range left is the tip commit itself. Never
          # silently scan nothing: an empty range would pass this gate without
          # reading a byte, which is the failure mode the old job's comment
          # warned about.
          if [ -n "${BASE_SHA}" ]; then
            range="${BASE_SHA}..${HEAD_SHA}"
          elif [ -n "${BEFORE_SHA}" ] && [ "${BEFORE_SHA}" != "0000000000000000000000000000000000000000" ] \
               && git cat-file -e "${BEFORE_SHA}^{commit}" 2>/dev/null; then
            range="${BEFORE_SHA}..${HEAD_SHA}"
          else
            range="-1 ${HEAD_SHA}"
          fi
          echo "scanning ${range}"
          echo "log_opts=${range}" >> "$GITHUB_OUTPUT"

      - name: Detect secrets
        env:
          LOG_OPTS: ${{ steps.range.outputs.log_opts }}
        run: ./gitleaks git . --no-banner --redact --verbose --log-opts "${LOG_OPTS}"
